streamlit>=1.38
sqlalchemy>=2.0
pandas>=2.2
plotly>=5.22
python-docx>=1.1
fpdf2>=2.7.6
# Postgres driver — used when DATABASE_URL points at Postgres (Supabase, etc.)
psycopg2-binary>=2.9
# only used by the one-time migrate_to_supabase.py script
openpyxl>=3.1
